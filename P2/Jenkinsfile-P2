pipeline {
    agent any

    stages {
        stage('Making Project2') {
            steps {
                echo 'Creating..'
               // Make the output directory.
                sh "mkdir -p output"
                sh "cd P2 && ls -l"
                sh "pwd"
                sh "tree"
                sh "cd .. && pwd"
                }
            }
        }
}
